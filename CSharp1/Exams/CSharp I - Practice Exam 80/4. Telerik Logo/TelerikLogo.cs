using System;

class TelerikLogo
{
    static void Main()
    {
        char[,] logo;
        byte sideX = byte.Parse(Console.ReadLine());
        for (int line = 1; line <= sideX * 3 - 2; line++)
        {
            for (int col = 1; col <= sideX * 3 - 2; col++)
            {
                
            }
        }
    }
}